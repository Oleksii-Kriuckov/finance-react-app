import React from 'react';
import { Form, FormControl } from 'react-bootstrap'

const SearchTickers = () => {
  return <div>
    <Form>
      <FormControl placeholder='search ticker' />
    </Form>
  </div>;
};

export default SearchTickers;
